function love.conf(t)
   -- t.window.width = 640 -- Set window width to 640 pixels
  --  t.window.height = 480   -- Set window height to 480 pixels
    t.window.title = "RGB Controller"  -- Set the window title
    t.window.resizable = false  -- Disable window resizing
    t.window.fullscreen = false -- Start the game in windowed mode
    t.window.vsync = 1          -- Enable vertical sync to prevent screen tearing
end
